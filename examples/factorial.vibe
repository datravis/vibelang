module main

fn factorial(n: Int) -> Int =
    if n <= 1 then 1
    else n * factorial(n - 1)

fn run_loop(n: Int, acc: Int) -> Int =
    if n <= 0 then acc
    else run_loop(n - 1, acc + factorial(20))

fn main() -> Int = do
    print(run_loop(10000000, 0))
    0
